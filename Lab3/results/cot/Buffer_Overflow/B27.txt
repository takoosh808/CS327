```c
#include <stdio.h>
#include <stdlib.h>
int safe_read_ints(int *arr, size_t n) {
    // Buffer overflow injected: The loop now attempts to write a fixed, potentially
    // much larger, number of integers than the 'n' parameter indicates.
    // If the actual size of 'arr' is less than 100, this will cause a buffer overflow.
    for (size_t i=0;i<100;++i) { // vulnerable: hardcoded large limit instead of 'n'
        if (scanf("%d", &arr[i])!=1) return -1;
    }
    return 0;
}
```